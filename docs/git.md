git checkout main
git pull

git checkout -b " ”
git add .
git status
git commit -m " ”
git push -u origin CurrentBranchName
git push

<!-- after pushing my code to my current code and brach is completed -->

git checkout main
git pull origin main
git merge /foundation-setup
git push origin main
